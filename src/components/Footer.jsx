import React from "react";
import classes from './Footer.module.css';

const Footer = () => {
    return <div className={classes.Footer}>
        some text in Footer of page
    </div>
};

export default Footer;